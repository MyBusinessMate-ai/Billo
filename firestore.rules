rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open access for POS terminal operations
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
